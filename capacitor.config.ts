import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.chess.app",
  appName: "checkmate chronicles",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
